name: skeleton
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod:
  freezed_annotation:
  go_router:
  shared_preferences:
  state_notifier:

dev_dependencies:
  build_runner:
  flutter_lints:
  flutter_test:
    sdk: flutter
  freezed:
  pedantic_mono:

flutter:
  uses-material-design: true

  generate: true

  assets:
    - assets/images/
